
///ex1
/*let Ndata=data.map(d => {return { email:d.email, companyName:d.company.name } })
console.log(Ndata)*/

//Ex2

/*let Ndata=data.filter(d=> (d.address.zipcode[0] == 5))
console.log(Ndata)*/

//Ex3

/*let Ndata=data.filter(d=> (d.address.zipcode[0] == 5))
console.log(Ndata.map(d=>d.id))*/

//Ex4

/*let Ndata=data.filter(d => d.name[0]==='C')
arrayName=Ndata.map(d=> d.name)
console.log(arrayName)*/

//Ex5
/*let Ndata=data.every(d=>d.address.city==='South Christy')
console.log(Ndata)*/

//Ex6
/*let userCompanyName=data.find(d=>d.address.suite==='Apt. 950')
console.log(userCompanyName.company.name)*/


//Ex7
/*data.forEach(userName)
function userName(user){

    console.log(`${user.name} lives in ${user.address.city} and owns the company ${user.company.name}`)
}*/



